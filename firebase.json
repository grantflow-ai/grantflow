{
	"functions": {
		"ignore": ["node_modules", ".git", "tests", "**/__pycache__"],
		"predeploy": [
			"rm -rf functions && cp -r ../functions functions && cd functions && python3.12 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
		],
		"runtime": "python312",
		"source": "functions"
	},
	"emulators": {
		"firestore": {
			"port": 8080,
			"host": "0.0.0.0"
		},
		"ui": {
			"enabled": true,
			"port": 4000,
			"host": "0.0.0.0"
		}
	}
}
