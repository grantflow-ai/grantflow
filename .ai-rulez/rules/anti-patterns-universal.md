---
priority: critical
---

# Anti-Patterns - Universal

Never: Any type, class-based tests, mocking internals, manual dependency management, blocking I/O in async, bare exception handlers, magic numbers, inheritance for reuse, global state, f-strings in logs.
