---
priority: critical
---

# Lock Files & Dependency Management

Commit all lock files (uv.lock, pnpm-lock.yaml, Cargo.lock, composer.lock). Minimize external dependencies with justification. Regular audits (npm/pip audit). Version sync across all runtimes in respective config files.
