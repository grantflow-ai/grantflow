# pytest-xdist test groups for tests that need to run in the same worker
# This helps avoid conflicts with shared resources like databases

# Database migration tests should run together
services/*/tests/db_*
services/*/tests/*_migration*

# Tests that modify global state
services/*/tests/*_config*
services/*/tests/*_settings*