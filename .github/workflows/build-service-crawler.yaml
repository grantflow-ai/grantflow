name: "Build & Deploy: Crawler"

on:
    push:
        branches:
            - main
            - development
        paths:
            - ".github/workflows/build-service-crawler.yaml"
            - ".github/workflows/build-and-deploy-service.yaml"
            - ".github/actions/**"
            - "packages/db/**"
            - "packages/shared_utils/**"
            - "services/crawler/**"
            - "pyproject.toml"
            - "uv.lock"
    release:
        types: [published]
    workflow_dispatch:

jobs:
    build-and-deploy:
        uses: ./.github/workflows/build-and-deploy-service.yaml
        with:
            service: crawler
            timeout: 30

