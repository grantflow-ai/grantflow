# Indexer Service

Document processing and indexing service for RAG retrieval.

## Purpose

Processes uploaded documents, extracts text, chunks content, generates embeddings, and stores them in PostgreSQL with pgvector for similarity search.
